# 内容规则

这份文件是**给未来的我（或下一个会话）看的**。这个项目的会话很长，规则靠记忆一定会丢。
用户改动规则时，改这里。

## 1. 一个分号 = 一个 sum

用户发来的总结按**分号**切分。**每个分号之间是一个 sum（一个知识单元）**，不是一篇文章。

> 反例：把 4 个用分号隔开的主题扩写成"5 个 h2 + 2 张表 + 2 个大交互 + 5 道自测"——用户原话是「太乱」。

## 2. 每个 sum 固定三段式

1. **原话** —— 用户的原话**照贴，一字不改**（用引用块）
2. **纠正** —— 修正版：符号统一到 Franklin 原书、补全、纠错
3. **几何 / 直观交互图 + 一句「读图提示」** —— 图下面紧贴**一两句**，说"看图看哪里"，**不复述公式**

**符号矩阵、公式表降级到图下面**，缩成一小块，作为"要背下来的形式"。顺序反了就会乱。

**几何量 ↔ 时域量必须同色对应。** 说"σ 决定衰减"是**断言**，不是展示——
要在图上把 σ 和它管的那个时域量（ts、包络）用**同一个颜色**画出来，让对应关系可见，
并在图例与读数里写明颜色，色觉障碍读者也能用。

> 用户在 SUM 2 上指出：「有体现 s 域的实部虚部夹角对应时域指标吗」——
> 当时的图只标了四个几何量的名字，对应关系全在正文里用文字说，等于没做到。

**不要为"直观理解"另开第四段。** 图不自明就**改图**（加标注、加引导线、把关键量标在图上），
不要在下面补一篇文章——那既是你说的"太乱"，也是"臃肿"的来源。

## 3. 优先几何 / 直观理解

做交互之前先问：**这条有没有几何或直观的表示？**

- 优先：框图、积分器链、平面图（s 平面）、向量、时间轴、几何构造
- 符号矩阵**不是主角**
- **仿真波形类仍然用 canvas** —— 不必为了"避开 canvas 的坑"而强行用 DOM/SVG

## 3.5 数值必须可溯源

图上出现的**每一个数**，都要能在图上或推导链里找到出处。

> 反例：Mason 流图的支路只标字母 $a,b,c,e$，读数却直接给出 $P_1=16$——中间那步 $a=b=c=e=2$ 从未出现，用户原话是「这个 16 意义不明」。

做法：
- **图上直接标值**（`a = 2` 而不是 `a`）
- **读数给完整代入链**（`abce = 2×2×2×2 = 16`，而不是 `16`）
- 中间量单独列一行并说明来历（如 $\Delta_1=1$、$\Delta_2=0.25$ 要靠"接不接触"解释）
- 未走到的步骤显示「？」，不要提前把答案漏出来

## 4. 页面边界由用户定

什么时候另起一页 / 另起一个子节，**由用户决定**。不要自作主张拆页。

## 4.5 框架冻结

**不再增加新交互原语**，除非三条同时满足：

1. 某个**具体的 sum** 提出了需求；
2. 现有原语确实做不出来；
3. 用户**先确认了几何形状**。

现有原语（11 个）：`step-2nd` `step-presets` `polezero-drag` `bode-cursor` `root-locus` `mason-flow` `integrator-chain` `canonical-pair` `step-metrics` `splane-geometry` `pole-catalog`。

> 理由：这个项目**内容是主体**。每加一个组件，用户就要在平板上多验一次，而内容一个字没增加。
> 已经发生过：d3 在第 9 轮被选来画图，后来在 canvas.js 里自己写了坐标轴，**再也没回头删**，
> 结果每个页面白下载 280KB、一直没人发现。

工具脚本同理：**只有能防止已经发生过的错误才加**。

## 4.6 提交前必须核对每一步改动真的成功了

**出过的事故**：一次 `edit` 返回 FAIL（我猜错了一个半径常数，old_string 没匹配上），
我没核对返回值就继续改了同组的读数与图例，然后在提交信息里写了"角度已改成 θ"——
结果**图和读数不一致，提交信息还谎报了改动**。

规则：
- 每一批 edit 的返回值都打印出来并**逐条确认**；
- **只要有一条 FAIL，就中止提交**，先修完再说；
- 提交信息**只写已验证的改动**，不写"应该已经改了"的东西。

## 5. 一手来源优先

- 用户拍来的原书页**以原书为准**，不要凭记忆写判据、定义、系数
- 已确认的 Franklin 口径：上升时间 $10\%\to90\%$ 且 $t_r\cong1.8/\omega_n$（式 3.49）；调节时间 $\pm1\%$ 判据 $t_s\cong4.6/\zeta\omega_n$
- 没有一手来源时，宁可写「我不确定」，不要编一个看起来很合理的数

## 5.5 组织的负担归我，不归用户

用户的原话：**「我感觉我要爆炸了，组织文章好难」**。

sections 怎么切、sum 怎么分、三段式的顺序、放哪一页、桌上摆哪张图——**这些决定全是我的活**。
用户负责提供**原始笔记**和**事后纠正**，不该被要求参与结构设计。

因此默认工作方式：
- **只写正文，不主动加新图。** 需要新图时，用**一条**消息问要不要（附几何描述），不要就跳过。
- **结构我定**，用户最多事后说「这里不对」。
- **规则文件不再增长**，除非再次踩到**同一个**坑。

如果用户明确说"停"或"我受不了了"，立即切到**纯文字模式**：不加组件、不加工具、不加规则。

## 6. 不要做的事

- 不要把提纲扩写成文章
- 不要为了"每个知识点都有交互"而造**假交互**（做不出交互就直说）
- 不要加用户没要求的抽象与配置
